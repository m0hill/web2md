name = "getmd"
main = "build/worker/shim.mjs"
compatibility_date = "2024-11-07"

route = { pattern = "scrape.mohil.dev/*", zone_name = "mohil.dev" }

[build]
command = "cargo install -q worker-build && worker-build --release"
